import React from 'react'

type FooterProps = {}

const Footer: React.FC<FooterProps> = ({}) => {
    return (
        <footer>
            <small style={{ margin: 'auto' }}>created by @easydev</small>
        </footer>
    )
}

export default Footer
